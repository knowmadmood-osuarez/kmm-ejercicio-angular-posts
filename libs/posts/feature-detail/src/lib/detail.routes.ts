import { Routes } from '@angular/router';

import { PostDetailPageComponent } from './post-detail-page.component';

export const DETAIL_ROUTES: Routes = [{ path: '', component: PostDetailPageComponent }];
